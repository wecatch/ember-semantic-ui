{{#if hasBlockParams}}
    {{yield (action "start") uploader}}
{{else}}
    <input type="file" name="file" >
    {{#unless autoUpload}}
        {{#ui-button loading=isUploading theme=theme action="start"}}upload{{/ui-button}}
    {{/unless}}
{{/if}}