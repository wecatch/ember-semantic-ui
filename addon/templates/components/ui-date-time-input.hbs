{{#if hasBlockParams}}
    {{yield (hash date=(component 'ui-date-input' value=d update=(action (mut d)))  hour=(component 'ui-select' options=hours value=h update=(action (mut h))) minute=(component 'ui-select' options=minutes value=m update=(action (mut m)) ))}}
{{else}}
    {{ui-date-input value=d update=(action (mut d)) }}
    {{ui-select options=hours value=h}}
    {{ui-select options=minutes value=m}}
{{/if}}
